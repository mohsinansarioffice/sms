VITE_API_URL=http://localhost:5000/api
# Idle timeout (ms) before auto logout. Default 30 minutes.
# VITE_IDLE_SESSION_MS=1800000
# How often to refresh the session while active (ms). Default 10 minutes; keep below access token TTL.
# VITE_SESSION_REFRESH_MS=600000
